#
# objectHierarchyChanger field configuration
#

# set to 1 to enable the plugin
enabled = 0

change_idno_rules = {
    
	documents = {
		# applies to:
		# TODO: currently only tested in 'ca_objects'!
		table = ca_objects,
		restrictToTypes = [fonds,collections,groups,subgroups,series,file,documents,document_parts],
	},
	
}